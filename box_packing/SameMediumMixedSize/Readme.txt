These tests cases are meant to verify different sized art can be packed together. Particularly, art requiring large boxes should be packed first in large boxes. Any remaining space in large boxes can be filled by standard sized art

Instructions: 
For each directory, fill in the expectOutput.json file with the correct expected output for the givein input file.

Ask the client if there are any interesting cases we might be missing and create those tests if needed.
